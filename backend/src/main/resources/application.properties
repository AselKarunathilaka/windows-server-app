server.port=${SERVER_PORT:19090}
spring.data.mongodb.uri=${MONGODB_URI}
spring.application.name=Intern Management
app.version=${APP_VERSION:1.0.0}

app.cors.allowed-origins=${ALLOWED_ORIGINS:http://localhost:15173,http://localhost:18080,http://127.0.0.1:15173,http://127.0.0.1:18080}

# Logging configuration
logging.file.name=${LOG_FILE:logs/app.log}
logging.level.root=INFO
